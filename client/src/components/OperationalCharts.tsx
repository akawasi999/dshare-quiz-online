import { trpc } from "@/lib/trpc";
import { Bar, BarChart, CartesianGrid, Cell, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
export default function OperationalCharts() { const { data, isLoading } = trpc.admin.analytics.useQuery(); const point = [{ label: "Nạp", value: data?.pointsTopUp ?? 0, fill: "#065BE5" }, { label: "Tiêu", value: data?.pointsConsumed ?? 0, fill: "#DE1264" }, { label: "Thưởng", value: data?.pointsRewarded ?? 0, fill: "#007453" }]; return <section className="mx-auto mb-6 grid max-w-6xl gap-5 lg:grid-cols-2"><div className="rounded-[24px] bg-white p-5"><p className="text-xs font-bold text-[#141432]">Dòng Point thực tế</p><div className="mt-4 h-56">{isLoading ? "Đang tải…" : <ResponsiveContainer><BarChart data={point}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="label" /><YAxis /><Tooltip /><Bar dataKey="value">{point.map(item => <Cell key={item.label} fill={item.fill} />)}</Bar></BarChart></ResponsiveContainer>}</div></div><div className="rounded-[24px] bg-white p-5"><p className="text-xs font-bold text-[#141432]">Lượt làm theo bộ đề</p><div className="mt-4 h-56">{isLoading ? "Đang tải…" : <ResponsiveContainer><BarChart data={data?.popularQuizzes ?? []}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="title" hide /><YAxis /><Tooltip /><Legend /><Bar dataKey="count" name="Lượt làm" fill="#3762D2" /><Bar dataKey="passRate" name="Tỷ lệ đạt" fill="#007453" /></BarChart></ResponsiveContainer>}</div></div></section>; }
