import { FileText, Settings2 } from "lucide-react";
import { useState } from "react";

export function QuizSettingsDrawer({ children }: { children: React.ReactNode }) {
  const [active, setActive] = useState<"questions" | "settings">("questions");
  const tabs = (overlay = false) => <div className="flex justify-center"><div role="tablist" aria-label="Khu vực Studio Quiz" className="inline-flex rounded-xl bg-[#f1f3f8] p-1 shadow-inner"><button type="button" role="tab" aria-selected={active === "questions"} onClick={() => setActive("questions")} className={`inline-flex min-w-36 items-center justify-center gap-2 rounded-[10px] px-5 py-2.5 text-xs font-bold transition ${active === "questions" ? "bg-white text-[#172554] shadow-[0_2px_8px_rgba(20,44,91,.12)]" : "text-[#8a95a5] hover:text-[#065be5]"}`}><FileText size={15} />Câu hỏi</button><button type="button" role="tab" aria-selected={active === "settings"} onClick={() => setActive("settings")} className={`inline-flex min-w-36 items-center justify-center gap-2 rounded-[10px] px-5 py-2.5 text-xs font-bold transition ${active === "settings" ? "bg-white text-[#172554] shadow-[0_2px_8px_rgba(20,44,91,.12)]" : "text-[#8a95a5] hover:text-[#065be5]"}`}><Settings2 size={15} />Cài đặt</button></div></div>;
  return <>{tabs()}{active === "settings" ? <section className="fixed inset-x-0 bottom-0 top-[76px] z-50 overflow-y-auto bg-[#f6f8fc] px-3 py-6 sm:px-5"><div className="mx-auto max-w-[1120px]">{tabs(true)}<div className="mt-5 rounded-2xl border border-[#172554]/10 bg-white p-5 shadow-[0_12px_35px_rgba(20,44,91,.08)]"><header className="mb-5 border-b border-[#172554]/10 pb-4"><h2 className="flex items-center gap-2 text-base font-bold text-[#172554]"><Settings2 size={18} className="text-[#065be5]" />Cài đặt Quiz</h2><p className="mt-1 text-xs leading-5 text-[#71838d]">Thiết lập chủ đề, ảnh bìa, thời lượng và bảo mật trước khi lưu Quiz.</p></header><div className="space-y-5">{children}</div></div></div></section> : null}</>;
}
