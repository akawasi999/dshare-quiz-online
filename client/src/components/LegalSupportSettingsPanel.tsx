import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { defaultPrivacyContent, defaultSupport, defaultTermsContent, formatUpdatedAt } from "@/lib/legalContent";
import { trpc } from "@/lib/trpc";
import { FileText, Headphones, Mail, Save, ShieldCheck } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
import { toast } from "sonner";

type LegalDraft = { terms: string; privacy: string; supportTitle: string; supportDescription: string; supportEmail: string; supportPhone: string; supportHours: string };

export default function LegalSupportSettingsPanel() {
  const utils = trpc.useUtils();
  const settings = trpc.admin.siteSettings.useQuery();
  const [draft, setDraft] = useState<LegalDraft>({ terms: defaultTermsContent, privacy: defaultPrivacyContent, supportTitle: defaultSupport.title, supportDescription: defaultSupport.description, supportEmail: defaultSupport.email, supportPhone: defaultSupport.phone, supportHours: defaultSupport.hours });
  useEffect(() => { const item = settings.data?.settings; if (item) setDraft({ terms: item.termsContent || defaultTermsContent, privacy: item.privacyContent || defaultPrivacyContent, supportTitle: item.supportTitle || defaultSupport.title, supportDescription: item.supportDescription || defaultSupport.description, supportEmail: item.supportEmail || "", supportPhone: item.supportPhone || "", supportHours: item.supportHours || defaultSupport.hours }); }, [settings.data?.settings]);
  const saveLegal = trpc.admin.saveLegalContent.useMutation({ onSuccess: () => { utils.admin.siteSettings.invalidate(); utils.site.legalSupport.invalidate(); toast.success("Đã lưu nội dung pháp lý."); }, onError: error => toast.error("Không thể lưu nội dung pháp lý", { description: error.message }) });
  const saveSupport = trpc.admin.saveSupportContent.useMutation({ onSuccess: () => { utils.admin.siteSettings.invalidate(); utils.site.legalSupport.invalidate(); toast.success("Đã lưu thông tin hỗ trợ."); }, onError: error => toast.error("Không thể lưu thông tin hỗ trợ", { description: error.message }) });
  const saveDocument = (document: "terms" | "privacy") => saveLegal.mutate({ document, content: document === "terms" ? draft.terms : draft.privacy });
  const submitSupport = (event: FormEvent) => { event.preventDefault(); saveSupport.mutate({ title: draft.supportTitle, description: draft.supportDescription, email: draft.supportEmail.trim() || null, phone: draft.supportPhone.trim() || null, hours: draft.supportHours.trim() || null }); };
  const item = settings.data?.settings;
  return <div className="mt-6 space-y-5"><div className="grid gap-5 xl:grid-cols-2"><section className="overflow-hidden rounded-[var(--radius-xl-token)] border border-border bg-surface"><div className="flex items-start gap-3 p-6"><span className="grid size-10 place-items-center rounded-[var(--radius-md-token)] bg-primary-light text-primary"><FileText size={19} /></span><div><h2 className="text-xl font-bold text-foreground">Điều khoản sử dụng</h2><p className="mt-1 text-sm text-text-secondary">Nội dung hiển thị tại /terms.</p></div></div><div className="px-6 pb-6"><Textarea aria-label="Nội dung Điều khoản sử dụng" value={draft.terms} onChange={event => setDraft(current => ({ ...current, terms: event.target.value }))} className="min-h-72 bg-background leading-6" /><p className="mt-3 text-xs text-text-secondary">Cập nhật lần cuối: {formatUpdatedAt(item?.termsUpdatedAt)}</p></div><footer className="flex justify-end border-t border-border bg-muted/35 px-6 py-4"><Button onClick={() => saveDocument("terms")} disabled={saveLegal.isPending || draft.terms.trim().length < 40}><Save size={15} />{saveLegal.isPending ? "Đang lưu…" : "Lưu Điều khoản"}</Button></footer></section><section className="overflow-hidden rounded-[var(--radius-xl-token)] border border-border bg-surface"><div className="flex items-start gap-3 p-6"><span className="grid size-10 place-items-center rounded-[var(--radius-md-token)] bg-primary-light text-primary"><ShieldCheck size={19} /></span><div><h2 className="text-xl font-bold text-foreground">Chính sách bảo mật</h2><p className="mt-1 text-sm text-text-secondary">Nội dung hiển thị tại /privacy.</p></div></div><div className="px-6 pb-6"><Textarea aria-label="Nội dung Chính sách bảo mật" value={draft.privacy} onChange={event => setDraft(current => ({ ...current, privacy: event.target.value }))} className="min-h-72 bg-background leading-6" /><p className="mt-3 text-xs text-text-secondary">Cập nhật lần cuối: {formatUpdatedAt(item?.privacyUpdatedAt)}</p></div><footer className="flex justify-end border-t border-border bg-muted/35 px-6 py-4"><Button onClick={() => saveDocument("privacy")} disabled={saveLegal.isPending || draft.privacy.trim().length < 40}><Save size={15} />{saveLegal.isPending ? "Đang lưu…" : "Lưu Bảo mật"}</Button></footer></section></div><form onSubmit={submitSupport} className="overflow-hidden rounded-[var(--radius-xl-token)] border border-border bg-surface"><div className="flex items-start gap-3 p-6"><span className="grid size-10 place-items-center rounded-[var(--radius-md-token)] bg-primary-light text-primary"><Headphones size={19} /></span><div><h2 className="text-xl font-bold text-foreground">Liên hệ & Hỗ trợ</h2><p className="mt-1 text-sm text-text-secondary">Thông tin hiển thị tại /support và liên kết từ Footer chính.</p></div></div><div className="grid gap-5 px-6 pb-6 lg:grid-cols-2"><label><span className="text-sm font-semibold text-foreground">Tiêu đề trang</span><Input aria-label="Tiêu đề Liên hệ và Hỗ trợ" value={draft.supportTitle} onChange={event => setDraft(current => ({ ...current, supportTitle: event.target.value }))} className="mt-2 h-11 bg-background" required maxLength={180} /></label><label><span className="text-sm font-semibold text-foreground">Giờ hỗ trợ</span><Input aria-label="Giờ hỗ trợ" value={draft.supportHours} onChange={event => setDraft(current => ({ ...current, supportHours: event.target.value }))} className="mt-2 h-11 bg-background" maxLength={320} /></label><label className="lg:col-span-2"><span className="text-sm font-semibold text-foreground">Mô tả</span><Textarea aria-label="Mô tả Liên hệ và Hỗ trợ" value={draft.supportDescription} onChange={event => setDraft(current => ({ ...current, supportDescription: event.target.value }))} className="mt-2 min-h-24 bg-background" required maxLength={5000} /></label><label><span className="text-sm font-semibold text-foreground">Email hỗ trợ</span><span className="relative mt-2 block"><Mail className="absolute left-3 top-1/2 -translate-y-1/2 text-text-muted" size={16} /><Input aria-label="Email hỗ trợ" value={draft.supportEmail} onChange={event => setDraft(current => ({ ...current, supportEmail: event.target.value }))} className="h-11 bg-background pl-10" type="email" placeholder="support@dshare.vn" /></span></label><label><span className="text-sm font-semibold text-foreground">Số điện thoại</span><Input aria-label="Số điện thoại hỗ trợ" value={draft.supportPhone} onChange={event => setDraft(current => ({ ...current, supportPhone: event.target.value }))} className="mt-2 h-11 bg-background" placeholder="0123 456 789" maxLength={80} /></label></div><footer className="flex justify-between border-t border-border bg-muted/35 px-6 py-4"><span className="text-xs text-text-secondary">Cập nhật lần cuối: {formatUpdatedAt(item?.supportUpdatedAt)}</span><Button type="submit" disabled={saveSupport.isPending || draft.supportDescription.trim().length < 20}><Save size={15} />{saveSupport.isPending ? "Đang lưu…" : "Lưu thông tin hỗ trợ"}</Button></footer></form></div>;
}
