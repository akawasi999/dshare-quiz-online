import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Switch } from "@/components/ui/switch";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { Bot, KeyRound, Loader2, Save, ShieldCheck, Sparkles } from "lucide-react";
import { useEffect, useState } from "react";
import { toast } from "sonner";

export default function AdminAiAssistantPanel() {
  const config = trpc.aiAssistantAdmin.config.useQuery();
  const manusModels = trpc.aiAssistantAdmin.manusModels.useQuery(undefined, { enabled: config.data?.provider === "manus" });
  const [provider, setProvider] = useState<"manus" | "gemini">("manus");
  const [model, setModel] = useState("gpt-5-mini");
  const [apiKey, setApiKey] = useState("");
  const [isEnabled, setIsEnabled] = useState(false);
  const [welcomeMessage, setWelcomeMessage] = useState("");
  const save = trpc.aiAssistantAdmin.saveConfig.useMutation({
    onSuccess: () => { setApiKey(""); config.refetch(); toast.success("Đã lưu cấu hình AI Assistant.", { description: "API key được mã hóa và không hiển thị lại." }); },
    onError: error => toast.error("Không thể lưu cấu hình AI", { description: error.message }),
  });
  useEffect(() => {
    if (!config.data) return;
    setProvider(config.data.provider); setModel(config.data.model); setIsEnabled(config.data.isEnabled); setWelcomeMessage(config.data.welcomeMessage);
  }, [config.data]);
  const submit = () => save.mutate({ provider, model, apiKey: apiKey || undefined, isEnabled, welcomeMessage });
  const hasApiKey = config.data?.hasApiKey ?? false;
  return <main className="mx-auto max-w-5xl"><p className="text-[10px] font-bold uppercase tracking-[.17em] text-[#2563eb]">Dshare / Quản trị / AI Assistant</p><div className="mt-2 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="font-serif text-[36px] font-semibold tracking-[-.045em] text-[#172554]">AI Assistant</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#617786]">Cấu hình nhà cung cấp, mô hình và trạng thái hoạt động cho trợ lý học tập dùng chung trên Dshare.</p></div><Button onClick={submit} disabled={save.isPending || !welcomeMessage.trim() || !model.trim()} className="rounded-full bg-[#065be5] px-5">{save.isPending ? <Loader2 className="animate-spin" size={16} /> : <Save size={16} />}Lưu cấu hình</Button></div><div className="mt-7 grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><section className="rounded-[26px] border border-[#172554]/9 bg-white p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#eef4ff] text-[#065be5]"><Bot size={20} /></span><div><h2 className="font-serif text-xl font-semibold text-[#172554]">Kết nối mô hình</h2><p className="mt-1 text-[11px] text-[#71838d]">Chỉ quản trị viên có thể thay đổi cấu hình này.</p></div></div><div className="mt-6 grid gap-4"><div><Label>Nhà cung cấp</Label><div className="mt-2 grid grid-cols-2 gap-2"><button type="button" onClick={() => { setProvider("manus"); if (!model) setModel("gpt-5-mini"); }} className={`rounded-xl border p-4 text-left ${provider === "manus" ? "border-[#065be5] bg-[#eef4ff]" : "border-[#172554]/10 bg-white"}`}><p className="text-sm font-bold text-[#172554]">Manus AI</p><p className="mt-1 text-[10px] leading-4 text-[#617786]">Dùng khóa quản lý an toàn của hệ thống Manus.</p></button><button type="button" onClick={() => { setProvider("gemini"); if (model === "gpt-5-mini") setModel("gemini-2.0-flash"); }} className={`rounded-xl border p-4 text-left ${provider === "gemini" ? "border-[#065be5] bg-[#eef4ff]" : "border-[#172554]/10 bg-white"}`}><p className="text-sm font-bold text-[#172554]">Google Gemini</p><p className="mt-1 text-[10px] leading-4 text-[#617786]">Dùng Gemini API key do quản trị viên cung cấp.</p></button></div></div><div><Label htmlFor="ai-model">Mã mô hình</Label><Input id="ai-model" list="manus-models" value={model} onChange={event => setModel(event.target.value)} className="mt-2" placeholder={provider === "gemini" ? "gemini-2.0-flash" : "gpt-5-mini"} />{provider === "manus" ? <datalist id="manus-models">{manusModels.data?.map(item => <option key={item} value={item} />)}</datalist> : <p className="mt-1.5 text-[10px] text-[#71838d]">Ví dụ: <strong>gemini-2.0-flash</strong> hoặc mô hình Gemini có trong Google AI Studio của bạn.</p>}</div>{provider === "gemini" ? <div><Label htmlFor="gemini-key">Gemini API key</Label><div className="relative mt-2"><KeyRound size={16} className="absolute left-3 top-3 text-[#71838d]" /><Input id="gemini-key" type="password" value={apiKey} onChange={event => setApiKey(event.target.value)} className="pl-9" placeholder={hasApiKey ? "Đã lưu khóa mã hóa — nhập để thay thế" : "Dán Gemini API key"} autoComplete="new-password" /></div><p className="mt-1.5 text-[10px] leading-4 text-[#71838d]">Khóa được mã hóa AES-256-GCM trước khi lưu và sẽ không hiển thị lại. {hasApiKey ? "Một khóa đã được lưu." : "Chưa có khóa được lưu."}</p></div> : <div className="rounded-xl bg-[#eef4ff] p-4 text-xs leading-5 text-[#617786]"><strong className="text-[#172554]">Manus AI không cần API key riêng.</strong> Hệ thống dùng khóa máy chủ quản lý bởi nền tảng; khóa này không được gửi về trình duyệt.</div>}<div><Label htmlFor="assistant-welcome">Lời chào mặc định</Label><Textarea id="assistant-welcome" value={welcomeMessage} onChange={event => setWelcomeMessage(event.target.value)} className="mt-2 min-h-24" maxLength={500} /></div></div></section><aside className="rounded-[26px] bg-[#172554] p-6 text-white"><Sparkles className="text-[#f4c352]" size={22} /><p className="mt-5 text-[10px] font-bold uppercase tracking-[.15em] text-[#bfdbfe]">Trạng thái dịch vụ</p><h2 className="mt-2 font-serif text-2xl font-semibold">AI Assistant {isEnabled ? "đang bật" : "đang tạm dừng"}</h2><p className="mt-3 text-xs leading-5 text-[#dbeafe]">Khi được bật, người dùng có quyền AI theo gói có thể mở Trợ lý AI từ thanh điều hướng cá nhân.</p><div className="mt-6 flex items-center justify-between rounded-2xl bg-white/10 p-4"><div><p className="text-xs font-bold">Cho phép người dùng sử dụng</p><p className="mt-1 text-[10px] text-[#bfdbfe]">Áp dụng quota AI của gói thành viên.</p></div><Switch checked={isEnabled} onCheckedChange={setIsEnabled} aria-label="Bật AI Assistant" /></div><div className="mt-5 rounded-2xl border border-white/10 p-4"><ShieldCheck size={18} className="text-[#72d5b1]" /><p className="mt-3 text-xs font-bold">Bảo vệ API key</p><p className="mt-1 text-[10px] leading-5 text-[#bfdbfe]">Giao diện chỉ nhận trạng thái đã có khóa; API key không bao giờ được trả về từ máy chủ hay hiển thị trong nhật ký.</p></div></aside></div></main>;
}
