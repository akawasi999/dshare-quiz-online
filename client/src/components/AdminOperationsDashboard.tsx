import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { AlertTriangle, ArrowRight, BarChart3, BookOpenCheck, CircleDollarSign, ClipboardList, FileWarning, Layers3, ListChecks, Loader2, Network, ScrollText, ShieldCheck, Sparkles, UsersRound } from "lucide-react";
import { Link } from "wouter";

const modules = [
  ["/quan-tri/noi-dung", "Nội dung bốn cấp", "Chủ đề, môn học, bài học và bộ đề", Layers3],
  ["/quan-tri/cau-hoi", "Ngân hàng câu hỏi", "Biên soạn đa dạng và cấu hình đáp án", ListChecks],
  ["/quan-tri/tao-de-ngau-nhien", "Tạo đề ngẫu nhiên", "Ma trận câu hỏi theo độ khó", Sparkles],
  ["/quan-tri/import-xuat", "Import / Export", "Trao đổi ngân hàng với CSV và Excel", ClipboardList],
  ["/quan-tri/nguoi-dung", "Người dùng & Point", "Phân hạng, khóa tài khoản, điều chỉnh Point", UsersRound],
  ["/quan-tri/point", "Sổ cái Point", "Theo dõi biến động và số dư sau giao dịch", CircleDollarSign],
  ["/quan-tri/bao-loi", "Duyệt báo lỗi", "Xử lý báo lỗi và bồi hoàn Point", FileWarning],
  ["/quan-tri/bao-cao", "Báo cáo vận hành", "Kết quả học tập, Point và nội dung phổ biến", BarChart3],
  ["/quan-tri/nhat-ky", "Nhật ký kiểm toán", "Lịch sử thao tác quản trị", ScrollText],
] as const;

export default function AdminOperationsDashboard() {
  const overview = trpc.admin.overview.useQuery();
  const analytics = trpc.admin.analytics.useQuery();
  const reports = trpc.admin.reports.useQuery();
  const pending = reports.data?.filter(item => item.report.status === "pending") ?? [];
  const error = overview.error || analytics.error || reports.error;
  const loading = overview.isLoading || analytics.isLoading || reports.isLoading;
  const cards = [
    ["Người dùng", overview.data?.users ?? 0, "Tổng tài khoản học tập", UsersRound],
    ["Bộ đề", overview.data?.quizzes ?? 0, "Đã tạo trong hệ thống", BookOpenCheck],
    ["Lượt hoàn thành", overview.data?.submitted ?? 0, "Bài đã nộp", ClipboardList],
    ["Báo lỗi chờ duyệt", overview.data?.pendingReports ?? 0, "Cần kiểm duyệt", FileWarning],
  ] as const;
  const refresh = () => { overview.refetch(); analytics.refetch(); reports.refetch(); };
  return <div className="mx-auto max-w-7xl"><header className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.17em] text-[#3182ce]">Dshare / Trung tâm điều hành</p><h1 className="mt-2 font-serif text-[38px] font-semibold tracking-[-.045em] text-[#2a4365]">Dashboard quản trị</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#617786]">Điều hành nội dung, người học, Point, kiểm duyệt và chất lượng học tập từ cùng một không gian.</p></div><div className="flex gap-2"><Button asChild variant="outline" className="rounded-full"><Link href="/quan-tri/nhat-ky">Xem nhật ký</Link></Button><Button asChild className="rounded-full bg-[#3182ce] hover:bg-[#2a4365]"><Link href="/quan-tri/noi-dung">Tạo nội dung <ArrowRight size={15} /></Link></Button></div></header>{error ? <section className="mt-7 rounded-[24px] border border-[#f0c5bd] bg-[#fff7f5] p-5 text-sm text-[#a8493e]"><strong>Chưa tải được một phần dữ liệu vận hành.</strong><p className="mt-1">{error.message}</p><Button onClick={refresh} className="mt-4 rounded-full bg-[#3182ce]">Thử lại</Button></section> : <><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, value, note, Icon]) => <div key={label} className="rounded-[22px] border border-[#2a4365]/10 bg-white p-5"><Icon size={19} className="text-[#3182ce]" /><p className="mt-4 text-[10px] font-bold uppercase tracking-[.14em] text-[#71838d]">{label}</p><p className="mt-1 font-serif text-[34px] font-semibold text-[#2a4365]">{loading ? "—" : value}</p><p className="mt-1 text-[11px] text-[#71838d]">{note}</p></div>)}</section><section className="mt-6 grid gap-5 xl:grid-cols-[1.12fr_.88fr]"><div className="rounded-[28px] bg-[#2a4365] p-6 text-white"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#9cd0f6]">Sức khỏe vận hành</p><h2 className="mt-2 font-serif text-[28px] font-semibold">Tín hiệu học tập trọng yếu</h2></div><Network className="text-[#4299e1]" size={23} /></div><div className="mt-7 grid gap-4 sm:grid-cols-3"><Signal label="Tỷ lệ đạt" value={`${analytics.data?.passRate ?? 0}%`} note="Theo bài đã nộp" /><Signal label="Point đã tiêu" value={`${analytics.data?.pointsConsumed ?? 0}`} note="Phí tham gia" /><Signal label="Point đã thưởng" value={`${analytics.data?.pointsRewarded ?? 0}`} note="Thưởng & bồi hoàn" /></div><div className="mt-7 rounded-2xl bg-white/10 p-4 text-xs leading-5 text-[#d2e5f1]"><CircleDollarSign className="mb-2 text-[#4299e1]" size={17} />Dòng Point và hiệu quả học tập được tổng hợp từ sổ cái và lượt làm bài hiện có.</div><Button asChild variant="outline" className="mt-5 rounded-full border-white/25 text-white hover:bg-white/10 hover:text-white"><Link href="/quan-tri/bao-cao">Mở báo cáo vận hành <ArrowRight size={14} /></Link></Button></div><div className="rounded-[28px] border border-[#2a4365]/10 bg-white p-6"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#3182ce]">Hàng đợi cần xử lý</p><h2 className="mt-2 font-serif text-[28px] font-semibold text-[#2a4365]">Kiểm duyệt báo lỗi</h2></div><span className="rounded-full bg-[#ebf8ff] px-3 py-2 text-xs font-bold text-[#3182ce]">{pending.length} chờ duyệt</span></div>{reports.isLoading ? <div className="grid min-h-36 place-items-center"><Loader2 className="animate-spin text-[#3182ce]" /></div> : pending.length ? <div className="mt-5 space-y-3">{pending.slice(0, 3).map(item => <div key={item.report.id} className="rounded-2xl bg-[#ebf8ff] p-4"><p className="text-xs font-bold text-[#2a4365]">Câu hỏi #{item.report.questionId} · {item.reporter ?? "Học viên"}</p><p className="mt-2 line-clamp-2 text-[11px] leading-5 text-[#617786]">{item.report.details}</p></div>)}</div> : <div className="mt-5 rounded-2xl bg-[#ebf8ff] p-5 text-sm leading-6 text-[#617786]"><AlertTriangle className="mb-2 text-[#4299e1]" size={18} />Không có báo lỗi nào đang chờ kiểm duyệt.</div>}<Button asChild variant="outline" className="mt-5 rounded-full"><Link href="/quan-tri/bao-loi">Mở hàng đợi kiểm duyệt <ArrowRight size={14} /></Link></Button></div></section><section className="mt-6"><div className="flex items-end justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#3182ce]">Mô-đun quản lý</p><h2 className="mt-2 font-serif text-[28px] font-semibold text-[#2a4365]">Đi tới đúng tác vụ, đúng lúc.</h2></div><span className="hidden rounded-full bg-[#ebf8ff] px-3 py-2 text-xs font-bold text-[#3182ce] sm:block">{modules.length} mô-đun</span></div><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{modules.map(([href, title, note, Icon]) => <Link key={href} href={href} className="group rounded-[22px] border border-[#2a4365]/10 bg-white p-5 transition hover:-translate-y-0.5 hover:border-[#4299e1]/60 hover:shadow-sm"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[#ebf8ff] text-[#3182ce]"><Icon size={18} /></span><h3 className="mt-4 text-sm font-bold text-[#2a4365]">{title}</h3><p className="mt-2 min-h-10 text-[11px] leading-5 text-[#617786]">{note}</p><span className="mt-4 inline-flex items-center gap-1 text-xs font-bold text-[#3182ce]">Mở mô-đun <ArrowRight size={13} className="transition group-hover:translate-x-0.5" /></span></Link>)}</div></section></>}</div>;
}

function Signal({ label, value, note }: { label: string; value: string; note: string }) { return <div className="rounded-2xl bg-white/10 p-4"><p className="text-[10px] uppercase tracking-[.12em] text-[#a9cce6]">{label}</p><p className="mt-2 font-serif text-3xl font-semibold">{value}</p><p className="mt-1 text-[10px] text-[#c8deeb]">{note}</p></div>; }
